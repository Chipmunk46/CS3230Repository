package com.github.AustinWagstaffWeber.CS3230Repo;

import com.github.AustinWagstaffWeber.CS3230Repo.Math;

import java.util.Scanner;

public class Main {

    public static void main(String args[]){
        DestinyInventory di = new DestinyInventory();
        di.run();
        //CLI.init();
        /*
        String user_name = "tomatomouse69";
        Data d = new Data();
        d.getItemImage(d.getCharacterItem(
                0,
                d.getMemberType(user_name),
                d.getMemberID(user_name),
                d.getCharacter(0,
                        d.getMemberType(user_name),
                        d.getMemberID(user_name)
                )
        ));
        */

        //System.out.println("memberid: " + d.getMemberID(user_name));
        /*
        d.getCharacterItem(
                0,
                d.getMemberType(user_name),
                d.getMemberID(user_name),
                d.getCharacter(0,
                    d.getMemberType(user_name),
                    d.getMemberID(user_name)
                    )
            );
        */

    }






}